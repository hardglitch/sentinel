[package]
name = "sentinel"
version = "0.1.4"
edition = "2024"
authors = ["hardglitch"]

[dependencies]
configparser = "3.2"
walkdir = "2.5"

[profile.release]
strip = true
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
